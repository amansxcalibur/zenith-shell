## Gray installation

clone https://github.com/Fabric-Development/gray?tab=readme-ov-file </br>
apt install `valac` and `libdbusmenu-gtk3-dev` </br>
cd into `/gray` and `meson --prefix=/usr build && sudo ninja -C build install`